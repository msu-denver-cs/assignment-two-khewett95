<%= form_with(model: car, local: true) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :make_name %>
    <%= form.text_field :make_name %>
  </div>

  <div class="field">
    <%= form.label :make_country %>
    <%= form.text_field :make_country %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model %>
  </div>

  <div class="field">
    <%= form.label :vin %>
    <%= form.text_field :vin %>
  </div>

  <div class="field">
    <%= form.label :parts %><br>
    <%= select_tag "part", options_for_select([['transmission'],['battery'],['radiator']], multiple: true) %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
